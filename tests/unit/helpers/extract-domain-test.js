import { extractDomain } from 'hackernews/helpers/extract-domain';
import { module, test } from 'qunit';

module('Unit | Helper | extract domain');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = extractDomain([42]);
  assert.ok(result);
});
